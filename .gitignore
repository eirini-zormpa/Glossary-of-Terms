# Ignore the _build folder output from the Jupyter Book build process.
# The HTML files generated by that process are automatically committed
# to the gh-pages branch by a GitHub Actions workflow instead.
**/_build/**
